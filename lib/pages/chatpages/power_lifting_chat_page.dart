import 'package:flutter/material.dart';

class PowerLiftingChatPage extends StatelessWidget {
  const PowerLiftingChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Center(
          child: Text('Power Lifting'),
        ));
  }
}
