import 'package:flutter/material.dart';

class BodyBuildingChatPage extends StatelessWidget {
  const BodyBuildingChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Center(
          child: Text('Body Building'),
        ));
  }
}