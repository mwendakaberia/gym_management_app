import 'package:flutter/material.dart';

class StrongManChatPage extends StatelessWidget {
  const StrongManChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(),
        body: Center(
          child: Text('Strong Man'),
        ));
  }
}