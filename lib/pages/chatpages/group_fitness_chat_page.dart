import 'package:flutter/material.dart';

class GroupFitnessChatPage extends StatelessWidget {
  const GroupFitnessChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(),
        body: Center(
          child: Text('Group Fitness'),
        ));
  }
}
