import 'package:flutter/material.dart';

class VideoLibraryPage extends StatelessWidget {
  const VideoLibraryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Video Library'),
    );
  }
}
